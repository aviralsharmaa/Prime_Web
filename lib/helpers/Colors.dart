import 'package:flutter/material.dart';

const MaterialColor primaryColors = MaterialColor(
  0xFF30475E,
  <int, Color>{
    50: primaryColor,
    100: primaryColor,
    200: primaryColor,
    300: primaryColor,
    400: primaryColor,
    500: primaryColor,
    600: primaryColor,
    700: primaryColor,
    800: primaryColor,
    900: primaryColor,
  },
);

const Color primaryColor = Color(0xFFB0AFAF);
const Color backgroundColorLightTheme = Color(0xFFF5F5F5);
const Color backgroundColorDarkTheme = Color(0xFF505050);
const Color accentColor = Color(0xFFFF9082);
const Color splashBackColor = Color(0xFFB0AFAF);
const Color whiteColor = Color(0xFFFFFFFF);
const Color blackColor = Color(0xFF000000);